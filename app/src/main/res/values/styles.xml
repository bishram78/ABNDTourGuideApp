<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- BASE APPLICATION THEME. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- BASE COLOR FOR THE TOOLBAR. -->
        <item name="colorPrimary">@color/colorPrimary</item>

        <!--BASE COLOR FOR THE STATUS BAR. -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <!-- BASE COLOR FOR OTHER VIEWS AND WIDGETS LIKE CHECKBOXES ETC. -->
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- CUSTOM ACTION BAR STYLE. -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- CUSTOM TOOLBAR STYLE. -->
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- NO ACTION BAR CUSTOM THEME. -->
    <style name="NoActionBarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <!--FULLSCREEN CUSTOM THEME. -->
    <style name="FullscreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- STYLE FOR THE RATING BAR VIEW. -->
    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@android:color/holo_green_dark</item>
        <item name="colorControlActivated">@android:color/holo_green_dark</item>
    </style>

    <!-- STYLE FOR A TAB THAT DISPLAYS A CATEGORY NAME. -->
    <style name="CategoryTab" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@android:color/white</item>
        <item name="tabSelectedTextColor">@android:color/white</item>
        <item name="tabTextAppearance">@style/CategoryTabTextAppearance</item>
    </style>

    <!-- TEXT APPEARANCE STYLE FOR A CATEGORY TAB. -->
    <style name="CategoryTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/tabText</item>
    </style>
</resources>
